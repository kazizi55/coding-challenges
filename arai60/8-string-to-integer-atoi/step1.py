# 時間切れ
class Solution:
    def myAtoi(self, s: str) -> int:
        answer = "0"
        stripped_s = s.strip()
        # for i in range(len(stripped_s)):
        #     if i == 0:
        return int(answer)
